import React from 'react';
import ReactTypingEffect from 'react-typing-effect';

const IntroText = () => {
    return(
        <h1>
            <ReactTypingEffect text={["hi! I'm Anna"]} />
        </h1>
    );
};

export default IntroText;